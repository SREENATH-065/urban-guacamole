import type { User, Message } from './types';

// This file is no longer used as the app is now connected to Firebase.
// It is kept to prevent breaking any potential unused imports.

export const MOCK_USERS: User[] = [];

export const MOCK_MESSAGES: Message[] = [];
